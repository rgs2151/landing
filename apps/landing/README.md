# Landing

Owned React/Vite app for `singha.io`.

## Local Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The Docker image builds the Vite app and serves the static `dist/` output with nginx.
